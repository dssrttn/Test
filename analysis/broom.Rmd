---
title: "broom"
author: "Mein Vorname"
date: "2020-06-27"
output: workflowr::wflow_html
editor_options:
  chunk_output_type: console
---

## Introduction

```{r echo=FALSE}
#library(broom)
library(tidyverse)
library(broom)
library(knitr)

options(pillar.sigfig=5)

DIL_tidy <- read_csv2('data/DIL_1.csv')#, locale(encoding = "UTF-8"), col_names = TRUE, col_types = NULL)
                      #, header=TRUE, stringsAsFactors=FALSE, sep=";", dec=",", row.names="Dosisname") #read_csv2: fuer europaeische Tabellen!

DIL_tidy

names(DIL_tidy)
#print.data.frame(DIL_tidy)

str(DIL_tidy)

summary(DIL_tidy$Mittelwert)

#tutorial: https://towardsdatascience.com/a-gentle-guide-to-statistics-in-r-ccb91cc1177e
ggplot(data = DIL_tidy, aes(x = Mittelwert)) + 
 geom_density(fill = "blue")

DIL_tidy %>% summarize(
 min = min(Mittelwert),
 max = max(Mittelwert)
 )

table(DIL_tidy$Mittelwert)

glimpse(DIL_tidy)

ggplot(data = DIL_tidy, # add the data
       aes(y = Mittelwert) 
       ) +  #, # set x, y coordinates
         #  color = drug_treatment)) +    # color by treatment
  geom_boxplot() 


#filter(DIL_tidy, between(Mittelwert, 74, 75))  


#summarise(hits = sum(between(DIL_tidy, 75, 77)))



#dplyr::filter(DIL_tidy, between(Mittelwert, 75, 77))

#tidy(DIL_tidy) 


```

