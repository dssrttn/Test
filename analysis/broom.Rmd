---
title: "broom"
author: "Mein Vorname"
date: "2020-06-27"
output: workflowr::wflow_html
editor_options:
  chunk_output_type: console
---

## Introduction

```{r echo=FALSE}
#library(broom)
library(tidyverse)
library(broom)
library(knitr)

options(pillar.sigfig=5)

DIL_tidy <- read_csv2('data/DIL_1.csv')#, locale(encoding = "UTF-8"), col_names = TRUE, col_types = NULL)
                      #, header=TRUE, stringsAsFactors=FALSE, sep=";", dec=",", row.names="Dosisname") #read_csv2: fuer europaeische Tabellen!

DIL_tidy

names(DIL_tidy)
#print.data.frame(DIL_tidy)

str(DIL_tidy)

summary(DIL_tidy$Mittelwert)

#tutorial: https://towardsdatascience.com/a-gentle-guide-to-statistics-in-r-ccb91cc1177e
ggplot(data = DIL_tidy, aes(x = Mittelwert)) + 
 geom_density(fill = "blue")

DIL_tidy %>% summarize(
 min = min(Mittelwert),
 max = max(Mittelwert)
 )

#tidy(DIL_tidy)


```

